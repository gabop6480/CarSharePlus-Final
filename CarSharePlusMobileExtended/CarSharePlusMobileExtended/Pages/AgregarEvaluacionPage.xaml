<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CarSharePlusMobileExtended.Pages.AgregarEvaluacionPage"
             Title="Nueva Evaluación">

    <VerticalStackLayout Padding="20" Spacing="10">
        <Label Text="Nueva Evaluación" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center" />

        <Label Text="Vehículo ID" />
        <Entry Text="{Binding VehiculoId}" Keyboard="Numeric" />

        <Label Text="Calificación (1-5)" />
        <Entry Text="{Binding Calificacion}" Keyboard="Numeric" />

        <Label Text="Comentario" />
        <Editor Text="{Binding Comentario}" HeightRequest="100" />

        <Button Text="Guardar Evaluación"
                Command="{Binding GuardarEvaluacionCommand}"
                Margin="0,20,0,0"
                BackgroundColor="#34C759"
                TextColor="White"
                CornerRadius="8" />
    </VerticalStackLayout>
</ContentPage>
